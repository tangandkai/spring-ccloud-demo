server:
  port: 8081

spring:
  application:
    name: spring-web-reactor
  cloud:
    nacos:
      discovery:
        server-addr: master:8848 #Nacos服务注册中心地址
      config:
        server-addr: master:8848 #Nacos作为配置中心地址
        file-extension: yaml #指定yaml格式的配置
        group: MY_GROUP
        namespace: 86cedb47-0cdf-45ad-aa20-84a282001817